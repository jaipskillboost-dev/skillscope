# Exact versions this project was built and tested with.
#
# Install with:  pip install -r requirements.txt
# (the Setup option in skillscope.bat does this for you)

Django==5.0.6
pillow==12.3.0

# Databases. PyMySQL is used on a laptop; psycopg is used on Render, which
# provides PostgreSQL. Both are installed so the same file works in both places.
PyMySQL==1.2.0
psycopg[binary]==3.3.5
dj-database-url==3.1.2

# Deployment
gunicorn==26.2.0          # the production web server (Linux only)
whitenoise==6.12.0        # serves CSS, JS and fonts in production
cloudinary==1.46.2        # stores uploads, since Render's disk is wiped

# Installed automatically as dependencies of the above. Pinned so everyone
# gets an identical setup.
asgiref==3.12.1
sqlparse==0.6.0
tzdata==2026.3
