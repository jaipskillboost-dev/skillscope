# Exact versions this project was built and tested with.
#
# Install with:  pip install -r requirements.txt
# (run.bat does this for you on the first run)

Django==5.0.6
PyMySQL==1.2.0
pillow==12.3.0

# Installed automatically as dependencies of the three above. Pinned so that
# everyone gets an identical setup.
asgiref==3.12.1
sqlparse==0.6.0
tzdata==2026.3
